# ── Internal AI/prompt tooling (not part of the website) ──
.github/prompts/

# ── OS / editor junk ──
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# ── Logs & temp ──
*.log
_shot_*.png
_*.ps1
tmp/
temp/

# ── Local env / secrets (never commit) ──
.env
.env.*
*.local
config.local.php
forms/config.php

# ── Composer dependencies (run `composer install` on server) ──
/vendor/
composer.lock
